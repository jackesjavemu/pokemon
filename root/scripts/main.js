const userName = window.prompt("what is your name");

console.log (userName);
if (username ==="admin"){
    const userName = window.prompt("what is your password");
    if (usepassword === "themaster"){
        console.log ("bienvenido");
    } else {
    console.log ("no te conozco");
} else {
    cost userPassword = window.prompt("what is your password?");
    
}